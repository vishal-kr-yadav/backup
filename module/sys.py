import sys

#print(sys.argv[0]) #it will show the path of current python script or you can mention argv only

#print(sys.builtin_module_names) #A tuple of strings giving the names of all modules that are compiled into this Python interpreter. (This information is not available in any other way — modules.keys() only lists the imported modules.)
